@Library('demo-shared-lib@feature/jenkinslib') _

pipeline {
    agent any

    stages {
        stage('Example') {
            steps {
                script {
                    MavenBuild()
                }
            }
        }
    }
}