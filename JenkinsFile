@Library('demo-shared-lib@feature/jenkinslib') _

pipeline {
    agent any

    stages {
        stage('Maven Build') {
            steps {
                script {
                    MavenBuild()
                }
            }
        }
    }
}